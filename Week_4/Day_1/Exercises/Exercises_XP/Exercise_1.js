// #1 : "inside the funcOne function 3" because the condition is true.

//#2
// inside the funcThree function 0
// inside the funcThree function 5

//#3 inside the funcFive function hello, window is the global object

//#4 inside the funcSix function test, because a inside the function overrides the global variable a

//#5 in the if block 5 // outside of the if block 2, because let creates a block scope variable,5.2 const would throw an error if you try to reassign it
